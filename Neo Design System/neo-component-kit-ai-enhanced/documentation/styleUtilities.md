The following classes are utilities that you can use in your application.

- Screenreader only
To hide something visually, but still have it read by screen readers.

NOTE: it's normal the example looks empty, if you want to have the content, grab your screen reader.
```js
<p class="screenreader-only">This is only read by screen readers</p>
```